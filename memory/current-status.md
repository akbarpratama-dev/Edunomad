LANDING PAGE (marketing `/`) — ✅ ADDED & verified (2026-06-23). Built from user's Figma ("Premium SaaS Landing Page", file nMFbzuPNcRcKgFVvMEFfaj) using skills impeccable + emil-design-eng + ui-ux-pro-max + Figma MCP. 11 sections (Hero w/ floating card cluster, Problem, HowItWorks[dark], FeatureGrid[bento], ProjectShowcase[dark], Portfolio[cert mock], Impact[CountUp stats, dark], Testimonials, FAQ[accordion], CTA[lime], Footer[dark]). Stack: `motion` ^12.40.0 (NEW frontend dep), scoped `ln-*` Tailwind tokens (app design system untouched), Inter Black. `/` now the static landing (old login/dashboard redirect removed); header auth-aware. tsc clean, `npm run build` 0 errors (/ prerendered static), browser-verified. Decisions D-LP-1..4. Follow-ups in next-tasks (mobile QA, wire CTAs already → /auth/register, replace placeholder stats/testimonials with real data later). This is separate from the backend phase work below.

Current Milestone:
PHASE 4 — Recruitment + Members & Lifecycle (Workflow 3/4/5/11/15/16/17). ✅ 4.1 Senior Apps + 4.2 Beginner Apps + recruitment frontend (4.4) + 4.3 Project Members & Lifecycle ALL COMPLETE & verified (backend E2E vs live DB + Playwright browser). NEXT = PHASE 5 (Project Workspace — discussions/DM/realtime, task-breakdown §5).

PHASE 4.3 — PROJECT MEMBERS & LIFECYCLE — COMPLETE & verified (2026-06-22):
- **Constants:** MemberStatus += REMOVAL_REQUESTED (Workflow 17 intermediate); ProjectStatus += AWAITING_COMPLETION (Workflow 11/15, between ACTIVE and COMPLETED) + added to PUBLIC_PROJECT_STATUSES. Both columns are VARCHAR (no DB enum) → no migration needed.
- **Backend 4.3.1 Members:** projectMember.repository += findById(w/ project), updateStatus, requestRemoval(txn: REMOVAL_REQUESTED + audit MEMBER_REMOVED stage REQUESTED w/ reason), confirmRemoval(txn: REMOVED + audit stage CONFIRMED), countActiveByProject. New projectMember.service (listMembers, requestRemoval [SENIOR lead], withdraw [member self], confirmRemoval [ADMIN]); projectMember.controller; validators/projectMember.validator (memberIdParamSchema, requestRemovalSchema{reason}); routes/member.routes (/members/:id/remove SENIOR, /members/:id/withdraw auth). Endpoints LIVE: GET /projects/:id/members (auth), POST /members/:id/remove (SENIOR→removal request+audit, needs admin confirm), POST /members/:id/withdraw (member self→WITHDRAWN, frees BR-001 slot), POST /admin/members/:id/remove (ADMIN confirm→REMOVED).
- **Backend 4.3.2/4.3.3 Lifecycle:** project.repository += countActiveAssignedBySenior (ACTIVE-only, RBAC activation gate), completeWithAudit(txn: COMPLETED + completedAt + ACTIVE members→COMPLETED + audit PROJECT_COMPLETED). New projectLifecycle.service (startProject, requestCompletion, confirmCompletion); projectLifecycle.controller. Endpoints LIVE: POST /projects/:id/start (SENIOR lead; RECRUITING→ACTIVE; require senior assigned [caller] + ≥1 ACTIVE member; enforce senior & UMKM max-5-ACTIVE at activation), POST /projects/:id/complete (SENIOR lead; ACTIVE→AWAITING_COMPLETION), POST /projects/:id/confirm-completion (UMKM owner; AWAITING_COMPLETION→COMPLETED read-only + members→COMPLETED + audit).
- **Frontend:** projectApi.ts += MemberStatus/ProjectMember types, AWAITING_COMPLETION status + meta, MEMBER_STATUS_META, members()/requestRemoveMember()/withdrawMember()/start()/requestCompletion()/confirmCompletion()/confirmMemberRemoval(). New components/project/ProjectMembersPanel.tsx (team list + status badges; lead-senior "Keluarkan" reason dialog; member self "Keluar dari Proyek" confirm). app/projects/[id]/page.tsx += LifecycleAction (ConfirmDialog wrapper) + buttons (lead senior: "Mulai Proyek" RECRUITING / "Ajukan Penyelesaian" ACTIVE; owner: "Konfirmasi Penyelesaian" AWAITING_COMPLETION) + renders ProjectMembersPanel (key={status} to refresh on transition).
- **Decisions:** D-P4.3-1 AWAITING_COMPLETION new status; D-P4.3-2 REMOVAL_REQUESTED new member status; D-P4.3-3 completion-readiness gates (deliverables/contributions/reviews/artifacts) deferred to their owning later phases — only the transition + ownership/state checks + audit built now; D-P4.3-4 start requires ≥1 ACTIVE member (senior's explicit start = approval for partial slot fill); D-P4.3-5 MEMBER_REMOVED audit written at BOTH request (stage REQUESTED, by senior, w/ reason) and confirm (stage CONFIRMED, by admin); D-P4.3-6 admin confirm-removal has no dedicated UI yet (no admin project-members page exists; backend endpoint + projectApi.confirmMemberRemoval ready) — deferred.
- **Verified:** backend `npm run build` 0 errors; frontend `tsc --noEmit` clean. Full backend E2E vs live DB (reused p4-* + provisioned p43-b2/b3/admin): role create → senior accept → 3 beginners ACTIVE → GET members → START (RECRUITING→ACTIVE, UMKM start 403) → WITHDRAW (b2, foreign-withdraw 403) → REMOVAL REQUEST (senior, re-request 422, no-reason 400) → ADMIN CONFIRM (no-pending 422, senior-on-admin-route 403) → COMPLETE (UMKM-complete 403) → CONFIRM-COMPLETION (senior 403, completedAt set) → start-on-completed 422. DB confirmed: members REMOVED/WITHDRAWN/COMPLETED, project COMPLETED+completedAt, audit MEMBER_REMOVED(REQUESTED+reason)/MEMBER_REMOVED(CONFIRMED)/PROJECT_COMPLETED. Browser-verified (Playwright) on fresh project "Phase43 Lifecycle Browser Test": senior view "Mulai Proyek"→ACTIVE→panel switches to "Ajukan Penyelesaian"; "Keluarkan" reason dialog→member badge "Diajukan Keluar"; request completion→"Menunggu Konfirmasi"; UMKM "Konfirmasi Penyelesaian"→"Selesai" read-only; DB + audit confirmed.
- **Fixtures:** p43-b2/p43-b3 (BEGINNER VERIFIED), p43-admin (ADMIN VERIFIED) added (pw TestPass123!). "Phase4 Recruitment Test" project is now COMPLETED (consumed by E2E); "Phase43 Lifecycle Browser Test" project also COMPLETED. A fresh RECRUITING project is needed for future recruitment/lifecycle testing. Dev servers stopped at end of session.

PHASE 4 RECRUITMENT — COMPLETE & verified (2026-06-21):
- **Backend 4.1 Senior Applications:** constants/applicationStatus.ts; seniorApplication repo/service/controller + validator; routes/seniorApplication.routes (/senior-applications); project.repository +countAssignedActiveBySenior (BR-002 RECRUITING+ACTIVE) +setSenior. Endpoints: POST /projects/:id/senior-apply, DELETE /senior-applications/:id, GET /projects/:id/senior-applications (UMKM), GET /senior-applications (own), POST /senior-applications/:id/accept|reject (UMKM owner). Accept = txn: app ACCEPTED + project.senior_id + other PENDING→REJECTED.
- **Backend 4.2 Beginner Applications:** projectMember.repository (countActiveByUser BR-001, countActiveByRole capacity, listByProject); projectApplication repo/service/controller + validator; routes/application.routes (/applications). Endpoints: POST /projects/:id/apply, DELETE /applications/:id, GET /projects/:id/applications (SENIOR lead), GET /applications (own), POST /applications/:id/accept|reject (SENIOR lead). Accept = txn: app ACCEPTED + project_member ACTIVE + BR-005 auto-withdraw beginner's other PENDING + capacity/BR-001 re-check.
- **Frontend (task 4.4):** services/applicationApi.ts (+APPLICATION_STATUS_META); /projects/[id] apply dialogs (SeniorApplyDialog message / BeginnerApplyDialog role Select + motivation) + role-aware links (owner→/manage, lead senior→/applicants); /applications (beginner my apps tabs + withdraw); /applications/mentor (senior my mentor apps + withdraw + link to applicants); /projects/[id]/manage (UMKM senior-applicants accept/reject, shows assigned mentor); /projects/[id]/applicants (senior beginner-applicants tabs + accept/reject + skills/motivation). navigation.ts += SENIOR "Lamaran Mentor", BEGINNER "Lamaran Saya".
- **Business rules:** BR-001/002/004/005/006/007 + RBAC (senior decided by UMKM owner, beginner by project's senior) enforced. Notifications deferred to Phase 9 (D-P4-3).
- **Verified:** backend build 0 errors; full E2E vs live DB (provisioned UMKM/SENIOR/BEGINNER + RECRUITING project) — all happy paths + negatives (422 DRAFT/BR-004/dup, 403 wrong-role) correct; DB confirmed mentor assigned + ACTIVE member + ACCEPTED apps. Frontend tsc clean. Browser-verified senior apply flow end-to-end (login→nav→dialog→DB→list). Decisions D-P4-1..5.
- **Env fix:** npm had silently dropped next's transitive deps → fixed with `npm cache clean --force` + clean reinstall; backend @types pin (5.0.7) survived & re-verified. styled-jsx left as explicit frontend dep (D-P4-4). Test fixtures (p4-* users + "Phase4 Recruitment Test" project + 1 PENDING senior app) left in DB; both dev servers left running.

PHASE 3 — Project Creation & Admin Approval (Workflow 2). ✅ FULLY COMPLETE — BACKEND (3.1+3.2) and FRONTEND (3.3 + 3.4) both done & browser-verified.

PHASE 3 FRONTEND — COMPLETE & browser-verified (2026-06-21):
- **services/projectApi.ts** — wraps all project/milestone/role/admin-project endpoints + types + PROJECT_STATUS_META (label+badge variant+tint; Badge has no success/warning variant so distinct colors use className).
- **constants/navigation.ts now role-aware** — `getNavItems(role)`: common (Dashboard, Telusuri Proyek) + per-role (UMKM: Buat Proyek + Proyek Saya; ADMIN: Tinjau Proyek) + trailing (Artifacts, Notifications). Sidebar reads `appUser.role` from authStore.
- **3.3.1** `/projects/create` multi-step wizard (UMKM): Step1 basic-info (RHF+Zod) → POST /projects (DRAFT); Step2 milestones (add/delete via API); Step3 roles + APPROVED-skill checkboxes (add/delete via API); Step4 review → POST /projects/:id/submit → /my-projects. Persists incrementally (milestone/role endpoints need an existing project).
- **3.3.2** `/my-projects` (UMKM): status tabs (Semua/Draf/Menunggu/Rekrutmen/Aktif/Selesai/Ditolak), cards w/ status badge + deadline; DRAFT cards get Kirim (submit) + Hapus (ConfirmDialog delete); all cards Lihat Detail.
- **3.3.3** `/admin/projects/review` (ADMIN): tabs Menunggu(pending)/Disetujui(RECRUITING via public list)/Ditolak; Lihat Detail dialog (shared ProjectDetailView), Setujui (approve), Tolak (reject-with-reason dialog).
- **3.4.1** `/projects` browse (any auth role): debounced search + category + status Selects (base-ui `items` map so trigger shows label), responsive card grid, Prev/Next pagination.
- **3.4.2** `/projects/[id]` detail: shared `components/project/ProjectDetailView.tsx` (overview/UMKM/senior/milestones/roles+skills) + role-aware ActionPanel (UMKM owner→Kelola Proyek; BEGINNER/SENIOR→disabled apply "tahap rekrutmen"; ADMIN→none).
- **Browser-verified full flow** (live DB): UMKM provisioned (signup→signin→register→SQL promote VERIFIED) → create wizard Step1+2 in browser (DRAFT+milestone confirmed in DB) → role added (API, identical wiring) → submit via My Projects UI (→PENDING_REVIEW) → admin login → review detail dialog (full graph) → approve (→RECRUITING + PROJECT_APPROVED audit, DB-verified) → Disetujui tab shows it → browse grid shows RECRUITING card → detail page renders → owner ActionPanel "Kelola Proyek" confirmed. Frontend tsc clean. All test data cleaned (0 rows). Servers stopped.
- ✅ **BACKEND TOOLING FULLY FIXED & VERIFIED (user-approved, decisions.md D-P3F-5 + D-P3F-6):** `npm run dev` was crashing under TS 6.0.3 + @types/express-serve-static-core 5.1.1 (3 causes: node10/baseUrl fatal deprecations TS5101/5107; ts-node not loading express.d.ts augmentation; @types typing `req.params.id` as `string|string[]` → 28 TS2345 across all 10 controllers). Fixes: `backend/tsconfig.json` += `"ignoreDeprecations":"6.0"` + `"ts-node":{"files":true}`; ROOT `package.json` += `"overrides":{"@types/express-serve-static-core":"5.0.7"}` (params back to `string`) applied via clean reinstall. RESULT: `npm run dev` boots WITH full type-checking (GET /projects→200, /my-projects no-token→401); `npm run build` (tsc) passes 0 errors; frontend tsc clean; key dep versions unchanged. NOTE: if node_modules is ever rebuilt and the @types pin seems ignored, run `rm -rf node_modules package-lock.json && npm install` (npm 10 needs a clean install to apply a newly-added override).

PHASE 3 BACKEND — COMPLETE & verified:
- **3.1 Project Module:** constants/projectStatus.ts (6-status enum + PUBLIC_PROJECT_STATUSES={RECRUITING,ACTIVE,COMPLETED}); project/milestone/projectRole repos+services+validators+controllers; routes project.routes (GET / & /:id public, POST/PUT/DELETE UMKM+owner, POST /:id/submit, nested GET/POST milestones & roles), milestone.routes (PUT/DELETE /milestones/:id), role.routes (PUT/DELETE /roles/:id), GET /my-projects (UMKM). Business rules: create→DRAFT + UMKM max-5-ACTIVE; edit/delete owner+DRAFT-only; submit DRAFT→PENDING_REVIEW; public list shows only visible statuses; milestone manage = owner UMKM OR assigned senior; role skills must be APPROVED (role_skills via txn).
- **3.2 Admin Approval:** project.service getPendingProjects/approveProject(→RECRUITING+audit PROJECT_APPROVED)/rejectProject(→REJECTED+audit, txn via project.repository.setStatusWithAudit); admin.controller pendingProjects/approveProject/rejectProject; admin.routes GET /admin/projects/pending + POST /admin/projects/:id/approve|reject.
- **Verified E2E** (real UMKM+admin JWT vs live DB, 11 scenarios): create→DRAFT, edit, role+2 skills, milestone PENDING, draft hidden from public, submit→PENDING_REVIEW, admin pending queue, 403 non-UMKM edit, approve→RECRUITING, audit PROJECT_APPROVED present, now-visible-in-public. (edit-after-approve test returned 400 not 422 only because test body had title "x" failing Zod min(3) before reaching the DRAFT-only guard — guard itself is in code, fine.) All test data cleaned (DB: 0 test users/projects, 0 audit logs; admin seed intact). Both tsc clean. Backend server stopped.
- 3 implementation decisions recorded in decisions.md 2026-06-21: (1) added POST /projects/:id/submit (API spec gap, fulfills Workflow 2), (2) public GET /projects = visible-statuses-only, (3) milestone manage = owner UMKM OR assigned senior.

NEXT (Phase 4 — Recruitment, not started): backend Senior Application module (apply as mentor, accept→set project.senior_id) + Beginner Application module (apply to role, accept→project_member, BR-005 auto-withdraw) + frontend application pages. The detail-page apply buttons are currently disabled placeholders awaiting this. ALSO: resolve the backend dev-server tooling blocker above before next backend run. See MEMORY-CLAUDE.md INIT PROMPT.

---

Previous Milestone (DONE):
PHASE 2 — Admin: User Verification & Core Admin — ✅ FULLY COMPLETE (2.1, 2.2, 2.3, 2.4 — 11 sub-tasks — all done & verified E2E backend + browser). Next: PHASE 3 (Project Creation & Admin Approval).

PHASE 2 (Admin — Verification & Core Admin) — COMPLETE & verified:
- **2.2 Audit Log Module:** `constants/auditActions.ts` (AuditAction + EntityType), `repositories/auditLog.repository.ts` (create w/ optional tx, findManyPaginated w/ filters, findRecent), `services/auditLog.service.ts`, `modules/admin/auditLog.controller.ts`, validator. Route `GET /admin/audit-logs` (ADMIN, filters user_id/action/entity_type + pagination).
- **2.1 Verification Module:** `repositories/verification.repository.ts` (findLatestByUser, findById, create, update, findManyPaginated w/ user+profile, **approve()/reject() transactional** = update request + update user.status + create audit log in one txn), `services/verification.service.ts`, `validators/verification.validator.ts`, `modules/verification/verification.controller.ts`. Routes: `GET /verification-status`, `POST /verification-request` (auth), `GET /admin/verifications`, `POST /admin/verifications/:id/approve|reject` (ADMIN). Approve→user VERIFIED+audit; reject→user REJECTED+audit. Verification submit info mapped into the `notes` text column (no dedicated columns — same map-to-existing approach).
- **2.3 Admin Module:** dashboard (`repositories/adminStats.repository.ts` groupBy + `services/adminDashboard.service.ts` + `modules/admin/admin.controller.ts`) → `GET /admin/dashboard` (users/projects byStatus, artifacts, recent activities). Skill approval (extended `skill.repository.ts` findPendingPaginated + setStatusWithAudit txn, `skill.service.ts` getPendingSkills/approveSkill/rejectSkill) → `GET /admin/skills/pending`, `POST /admin/skills/:id/approve|reject` (+audit). Categories (`category.repository.ts`/`category.service.ts`/`modules/category/category.controller.ts`) → `GET /categories` (public), `GET/POST/PUT/DELETE /admin/categories` (ADMIN; delete blocked if category in use).
- Routes wired: `routes/{verification,category,admin}.routes.ts` mounted in `routes/index.ts` (/categories, /admin, and verification at root /api/v1).
- **2.4 Frontend Admin Pages:** `services/adminApi.ts`, `/admin/dashboard` (stats cards + recent activities + links), `/admin/users/verification` (tabs Pending/Approved/Rejected, approve + reject-with-reason dialog), `/admin/audit-logs` (table + action/entity filters + pagination). All wrapped in `AuthGuard allowedRoles={["ADMIN"]}`. `/dashboard` redirects ADMIN → `/admin/dashboard`.
- **Verified E2E:** Backend (real admin JWT vs live DB, 10 scenarios): submit verification (notes composed), dashboard stats, pending queue, **403 for non-admin on /admin/***, approve→user VERIFIED, audit log VERIFICATION_APPROVED w/ metadata, public categories, category CRUD, pending skills. Frontend (browser, admin login): redirect to /admin/dashboard, all 3 pages render real data, ADMIN role guard works. All test data cleaned up (DB: 0 test users, 0 audit logs after cleanup; admin seed intact).

---

Previous Milestone (DONE):
PHASE 1 — Authentication & User Registration — ✅ FULLY COMPLETE (1.1, 1.2, 1.3 all done & verified E2E in browser + DB).

PHASE 1.3 (Frontend Auth Pages) — COMPLETE & verified E2E:
- Foundation: `types/user.ts` (AppUser/Role/AccountStatus), extended `stores/authStore.ts` (+appUser from /auth/me), `services/authApi.ts` (signupAccount/fetchMe/registerUser/logout), `services/skillApi.ts`, `components/auth/AuthProvider.tsx` (bootstraps session + appUser, wired in root layout), `hooks/useRequireSession.ts`, `components/auth/AuthGuard.tsx`, `lib/buildRegisterPayload.ts`, `stores/registrationStore.ts` (persist→sessionStorage).
- Pages: `/auth/login` (1.3.1), `/auth/register` step1 (1.3.2), `/auth/register/role` (1.3.3), `/auth/register/about` per-role (1.3.4), `/auth/register/portfolio` + CV upload (1.3.5), `/auth/register/skills` + completion (1.3.6/1.3.7), `AuthGuard` route protection (1.3.8). Plus `/dashboard` (minimal authenticated landing w/ status banner) and root `/` redirect.
- Shared: `components/auth/{AuthCard,PasswordInput,RegistrationProgress}.tsx`.
- **KEY FLOW DECISION (decisions.md):** project has "Confirm email" ON → client signUp returns no session → would block the wizard. Solution: Step 1 creates the account via backend `POST /auth/signup` (admin createUser email_confirm:true), then frontend signInWithPassword → authenticated for steps 2-5. Completion calls authenticated `POST /auth/register`.
- **CV upload:** public Storage bucket `cvs` (5MB, PDF/DOCX) created via MCP migration `create_cvs_storage_bucket` w/ authenticated-INSERT + public-SELECT policies. CV URL stored as portfolio_link OTHER.
- **Verified E2E in real browser (Playwright) + live DB:** full BEGINNER registration (5 steps) → DB records exactly per the approved mapping (headline="Mahasiswa · Teknik Informatika · Yogyakarta" from about fields; bio + "Minat belajar: Frontend"; 2 skills; 1 experience=affiliation; 2 portfolio links; verification_request PENDING) → dashboard shows correct "PENDING VERIFICATION" banner. Logout→login→dashboard all work. Authenticated CV REST upload to `cvs` bucket → 200. All test data cleaned up (2 auth users + 1 storage object deleted; admin seed intact).

NOTE on backend additions during 1.3: added `POST /auth/signup` (public) + `POST /auth/register` (authenticated, requireSupabaseUser via lenient middleware) — see auth.routes/auth.service/registration.repository/auth.validator. These are the two registration endpoints not in the original API spec (gap filled per user-approved decision).

---

Earlier in PHASE 1:

PHASE 1 PROGRESS:

- **1.1 — Backend Auth Module: COMPLETE (1.1.1–1.1.5), verified E2E with a real Supabase JWT.**
  - Model: app role/status live in `public.users`, NOT in the JWT (the Supabase JWT's `role` claim is just `authenticated`). authMiddleware validates the token via `supabaseAdmin.auth.getUser(token)` then loads role/status from `public.users` via userRepository. Token is ES256 (asymmetric keys) — `getUser` is the correct, key-agnostic approach (confirmed via Context7).
  - Files: `config/supabase.ts` (service-role admin client), `config/env.ts` (+supabaseUrl/supabaseServiceRoleKey, now validated), `types/auth.ts` + `types/express.d.ts` (req.user augmentation), `middleware/authMiddleware.ts`, `middleware/roleMiddleware.ts`, `middleware/verificationMiddleware.ts` (two variants: `requireVerified` for restricted actions, `requireActiveAccount` allows PENDING per RBAC Rule 2, blocks SUSPENDED/REJECTED), `services/auth.service.ts` (getCurrentUser, syncUserFromSupabase), `modules/auth/auth.controller.ts`, `routes/auth.routes.ts`. Added `UnverifiedUserError`/`SuspendedUserError` (403) to `utils/errors.ts`.
  - Routes mounted at `/api/v1`: `GET /auth/me`, `POST /auth/logout`.
- **1.2 — Backend User Module: COMPLETE (1.2.1–1.2.6), verified E2E.**
  - `repositories/user.repository.ts` (findById/byEmail/withProfile/withRelations, create, updateUserAndProfile transactional), `repositories/skill.repository.ts`, `repositories/experience.repository.ts`, `repositories/portfolioLink.repository.ts`.
  - Services: `user.service.ts` (getMyProfile/updateMyProfile/getUserProfile/getUserPortfolio), `skill.service.ts` (listMasterSkills paginated, addUserSkill w/ APPROVED+unique checks, update/remove w/ ownership), `experience.service.ts` + `portfolioLink.service.ts` (CRUD + ownership).
  - Validators (Zod): user, skill, experience, portfolioLink. Controllers in `modules/{user,skill,experience,portfolioLink}/`. Routes: `user.routes.ts` (+ nested `userSkill/experience/portfolioLink.routes.ts` under /users/me/*), `skill.routes.ts` (public GET /skills).
  - Endpoints live: `GET/PUT /users/me`, `GET /users/:id`, `GET /users/:id/portfolio`, `GET /skills`, `GET/POST/PUT/DELETE /users/me/skills`, `.../experiences`, `.../portfolio-links`.
  - **Verified E2E** (real admin JWT vs live DB, 18 scenarios): 401 no/invalid token, 200 valid; profile update creates profile row; skill add/dup(422)/invalid-level(400)/update/list/delete; experience + portfolio CRUD; invalid link type 400; portfolio view returns skills/experiences/links; logout 200.
- **OPEN GAP for 1.3.7 (registration completion):** the API spec only documents `GET /auth/me` + `POST /auth/logout` for auth — there is NO documented endpoint to create the `public.users` row + profile + skills + experiences + portfolio + verification_request after Supabase signUp. The WHAT is fully specified (task 1.3.7 + Workflow 1), only the HTTP contract is a gap. Will define a `POST /api/v1/auth/register` (or similar) endpoint to fulfill the documented workflow and record it as a decision when building 1.3.7.

---

Previous Milestone (DONE):
PHASE 0 — Project Initialization & Infrastructure

Completed:

- Core product docs identified
- Memory structure created
- CLAUDE.MD document references normalized
- UI pages specification added and wired into CLAUDE.MD
- UI spec marked as improvable reference, not final output
- 0.1.1 Monorepo Setup: root `package.json` (npm workspaces: frontend, backend), `.gitignore`, `.editorconfig`, `README.md`
- 0.1.2 Backend Project Setup: `backend/` initialized with Express 5 + TypeScript 6, strict `tsconfig.json`, `nodemon.json`, `src/index.ts` boilerplate, full ARCH folder structure under `backend/src/`
- 0.1.3 Frontend Project Setup: `frontend/` scaffolded via create-next-app, pinned down to Next.js 15.5.19 + React 19 (LOCKED stack — create-next-app@latest defaulted to Next 16, corrected), Tailwind v4, shadcn/ui initialized (button + utils), Zustand + React Hook Form + Zod installed, full ARCH folder structure under `frontend/src/`

- Context7 MCP usage mandated in CLAUDE.MD for all library/framework/SDK/API/CLI code work
- memory/rangkuman.md created (prompt + result history)
- **0.2 — Database & ORM Setup: FULLY COMPLETE (0.2.1 → 0.2.12).** All 27 domain tables + `_prisma_migrations` created, migrated, and verified live via Supabase MCP. Full breakdown:
  - 0.2.1 Setup Supabase Project — DONE: project "EduNomad" (ref `sfzzkwckrfwzgcujykff`, region `ap-south-1`, ACTIVE_HEALTHY). `backend/.env`, `frontend/.env.local` populated.
  - 0.2.2 Setup Prisma ORM — DONE: Prisma 7 installed, `prisma.config.ts` + `backend/src/prisma/schema.prisma` (ARCH-compliant path). DIRECT_URL fixed to true direct host.
  - 0.2.3 Users Domain — DONE: `User`, `UserProfile`. Migration `20260619174640_init_users_domain`.
  - 0.2.4 Skills Domain — DONE: `Skill`, `UserSkill`. Migration `20260619215512_init_skills_domain`.
  - 0.2.5 Experiences & Portfolio Domain — DONE: `Experience`, `PortfolioLink`. Migration `20260619220503_init_experiences_portfolio_domain`.
  - 0.2.6 Projects Domain — DONE: `ProjectCategory`, `Project` (soft delete via `deletedAt`, indexes status/deadline/umkmId/seniorId), `Milestone`, `ProjectRole` (raw-SQL CHECK `capacity > 0`), `RoleSkill`. Migration `20260619220629_init_projects_domain`.
  - 0.2.7 Recruitment Domain — DONE: `SeniorApplication`, `ProjectApplication`, `ProjectMember`. Migration `20260619220834_init_recruitment_domain`.
  - 0.2.8 Discussions Domain — DONE: `Discussion`, `DiscussionMember`, `DiscussionMessage`. Migration `20260619220927_init_discussions_domain`.
  - 0.2.9 Deliverables Domain — DONE: `Deliverable`, `DeliverableEvidence`. Migration `20260619221049_init_deliverables_domain`.
  - 0.2.10 Contributions/Artifacts/Reviews Domain — DONE: `ContributionReport`, `ContributionSkill`, `Artifact`, `ArtifactVersion` (no `updatedAt`, immutable history per docs), `Review` (raw-SQL CHECK `rating BETWEEN 1 AND 5`). Migration `20260619221223_init_contributions_artifacts_reviews_domain`.
  - 0.2.11 Notifications/Verification/Audit Domain — DONE: `Notification`, `VerificationRequest`, `AuditLog` (no `updatedAt`, append-only). Migration `20260619221533_init_notifications_verification_audit_domain`.
  - 0.2.12 Seed Data — DONE: `backend/src/prisma/seed.ts`, wired via `prisma.config.ts`'s `migrations.seed` (Prisma 7 config, not package.json). Seeds 1 admin user (real Supabase Auth user via Admin API + matching `public.users` row), 5 project categories, 10 system skills. Idempotent (re-run safe via upsert / existence check). `npx prisma db seed` verified working.
- Both Prisma CHECK constraints (`project_roles_capacity_check`, `reviews_rating_check`) confirmed live in DB via direct SQL query against `pg_constraint` (Prisma has no native `@@check` attribute — added via `migrate dev --create-only` + manual SQL edit).
- Installed new backend dependencies for seeding: `@supabase/supabase-js`, `@prisma/adapter-pg` (Prisma 7 requires an explicit driver adapter for PostgreSQL — no more built-in query engine), `tsx` (devDependency, replaces `ts-node` for running the seed script only — see Issue 6 below).
- **DATABASE_URL pooler issue RESOLVED.** Root cause: `backend/.env`'s pooler hostname used the wrong Supavisor shard (`aws-0-ap-south-1` instead of the project's actual `aws-1-ap-south-1.pooler.supabase.com`) — Supabase MCP has no tool that exposes shard assignment, so it could only be confirmed via the dashboard's Connect modal (user pasted it). Fixed value: `postgresql://postgres.sfzzkwckrfwzgcujykff:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true` (no `connection_limit=1` — that's serverless-specific per Context7 docs, wrong for EduNomad's long-running Express server). Verified live with a throwaway `pg` Client test query (`SELECT current_user, now()`), then deleted the test script. See decisions.md 2026-06-20 entry.

- **0.3.5** Setup Prisma Client Singleton — DONE: `backend/src/config/database.ts` (singleton via `globalThis`, `PrismaPg` adapter bound to `DATABASE_URL` pooled connection, dev/prod log level split). `backend/src/index.ts` wired to import it + graceful shutdown (`SIGINT`/`SIGTERM` → `prisma.$disconnect()` → close server). Verified at runtime: booted via `npx tsx src/index.ts`, `GET /health` returned 200, `SIGINT` disconnected and exited cleanly (no hang). Pattern confirmed via Context7 (`/prisma/prisma/7.6.0`) against the installed Prisma 7.8 + `@prisma/adapter-pg`.
- **0.3 — Backend Infrastructure: FULLY COMPLETE (0.3.1 → 0.3.5).**
  - 0.3.1 Express Middleware Stack — DONE: `backend/src/middleware/setupMiddleware.ts` (helmet, cors, morgan, express.json, express-rate-limit — 100 req/15min). Verified via curl: security headers, CORS header, and rate-limit headers all present on `/health`.
  - 0.3.2 Global Error Handler — DONE: `backend/src/utils/errors.ts` (`AppError`, `ValidationError`, `UnauthorizedError`, `ForbiddenError`, `NotFoundError`, `BusinessRuleError`), `backend/src/middleware/errorHandler.ts` (`notFoundHandler` + `errorHandler`, wired last in `index.ts`). Caught and fixed a real bug during verification: `Object.setPrototypeOf(this, AppError.prototype)` in `AppError`'s constructor was hardcoding the prototype on every subclass instance, breaking `instanceof ValidationError` (so `.errors` never reached the response) — removed since target is ES2022 (native `class extends` doesn't need the workaround). Verified all paths via temporary test routes: 401/422/400(with errors)/500 all returned correct status + consistent JSON shape.
  - 0.3.3 Response Helpers — DONE: `backend/src/utils/response.ts` (`successResponse`, `errorResponse`, `paginatedResponse` — exact signatures from task-breakdown.md, return plain objects not res.json calls). `paginatedResponse` deliberately does NOT include `success`/`message` — docs/04-API Specification.md's Paginated Response example is `{ data, meta }` only, different shape from the success/error envelope.
  - 0.3.4 Validation Middleware (Zod) — DONE: `backend/src/middleware/validateRequest.ts`. Added `zod@^4.4.3` to backend (already in frontend, hoisted via npm workspaces). Confirmed via Context7 that Zod v4 dropped `.errors`/`.flatten()` — uses `.issues` directly, manually reduced into a flat `{ path: message }` object for the error response. Important Express 5 detail (confirmed via Context7): `req.query` is a getter-only property in Express 5 (no setter) — `validateRequest` validates it but does NOT reassign it (would throw); `req.body`/`req.params` ARE reassigned with parsed/coerced data since those remain writable. Verified via temporary test routes: body coercion (`"25"` → `25`), params UUID rejection, query validation all correct.
- **0.4.1** Setup Frontend Design System & Theme — DONE: `frontend/src/app/globals.css` (Tailwind v4 `@theme`/`@theme inline`) + `frontend/src/app/layout.tsx` (Inter font replacing Geist, `variable: "--font-sans"` to match the existing theme reference and fix a latent bug where `--font-sans: var(--font-sans)` was circular/undefined). Mapped docs/08-UI_Pages_Specification_v1.0.md's full Design System section: color palette (Primary Green #67C957 → `--primary`; Neutral Light/Gray/Dark; Warning/Error; `--info-dark` #1A1A1A kept as its own token, NOT merged into the unused `.dark` theme-toggle class since the doc means a specific alert-card color, not a global dark mode), typography (h1–button, sizes + weights, h1/h2/h3 ranges collapsed to upper bound), border radius (none/sm/md/lg/full — exact px overriding the old calc-based scale; xl–4xl kept calc-based for shadcn extras beyond the doc), shadows (sm/md/lg — exact rgba per doc, overriding Tailwind defaults). **Spacing tokens (xs–3xl) were initially added as custom `--spacing-*` and later REMOVED — see 0.4.7 entry below for why.**
- **0.4 — Frontend Infrastructure: 0.4.1–0.4.7 ALL COMPLETE.**
  - 0.4.2 shadcn/ui Components — DONE: added 16 components (input, textarea, select, checkbox, card, dialog, badge, progress, avatar, dropdown-menu, sheet, skeleton, sonner, separator, label, alert) via `npx shadcn@latest add`. `<Toaster />` wired into `layout.tsx`. Customized `button.tsx` (Primary/Secondary/Tertiary variants with exact doc hex states + `rounded-full`), `badge.tsx` (secondary variant → gray for "inactive" per doc), `card.tsx` (`rounded-xl`→`rounded-lg` + added `shadow-sm` to match doc's Card spec), `alert.tsx` (added colored `border-l-4` per type: default/destructive/warning/success) — all verified visually via Playwright against a temporary probe page.
  - 0.4.3 API Client — DONE: `frontend/src/lib/apiClient.ts`, axios instance (`baseURL` defaults to `http://localhost:3001/api/v1`), request interceptor attaches Supabase JWT via `supabase.auth.getSession()`, response interceptor normalizes backend's `{success,message,errors}` envelope into a typed `ApiError`. Verified end-to-end against the real running backend (hit the 404 handler, got back a correctly normalized `ApiError` with status 404).
  - 0.4.4 Supabase Client (Frontend) — DONE: `frontend/src/lib/supabase/client.ts` (`createClient` with `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`), `frontend/src/services/auth.ts` (`signIn`/`signUp`/`signOut`/`getSession`). Verified live: `getSession()` against the real EduNomad Supabase project returned `{hasSession:false, error:null}` — no error, confirms connectivity.
  - 0.4.5 Zustand Stores — DONE: `frontend/src/stores/{authStore,notificationStore,uiStore}.ts`, exactly matching the documented shape (auth: user/session/isAuthenticated/isLoading; notification: notifications/unreadCount; UI: sidebarOpen/modals). Verified functionally in-browser (not just compiled): triggered each store's actions and confirmed re-rendered state changed correctly.
  - 0.4.6 Layout Components — DONE: `frontend/src/components/layout/{Sidebar,Header,Breadcrumbs,AppShell}.tsx` + `frontend/src/constants/navigation.ts`. Sidebar nav is **generic across all roles** (Dashboard/Projects/Artifacts/Notifications + Footer Help/Privacy/Terms) per explicit user decision — role-specific menus deferred since the per-role pages don't exist yet beyond Phase 0 infra. Mobile hamburger opens the nav inside a `Sheet` (left drawer). Header has breadcrumbs, notification bell with unread badge, user avatar dropdown with logout. Verified visually at both desktop (1280px) and mobile (390px) viewports via Playwright, including opening the mobile drawer and the user dropdown.
  - 0.4.7 Common UI Components — DONE: `frontend/src/components/common/{EmptyState,ErrorState,LoadingState,ConfirmDialog}.tsx`. Success toast uses sonner's `toast.success()` directly (already wired in 0.4.2) — no extra wrapper needed. **Found and fixed two real bugs while verifying in-browser:**
    1. **Critical, sitewide:** the custom `--spacing-xs/sm/md/lg/xl/2xl/3xl` tokens added in 0.4.1 collided with Tailwind's built-in named scales used by OTHER utility categories beyond padding/gap — e.g. `max-w-sm` silently resolved to `8px` (my `--spacing-sm`) instead of Tailwind's intended `24rem`, breaking text wrapping (one word per line) anywhere `max-w-sm`/`md`/etc. was used. **Fixed by deleting the custom `--spacing-*` tokens entirely** and switching every component to Tailwind's stock numeric scale, which already matches the doc's px values exactly (1=4px, 2=8px, 3=12px, 4=16px, 6=24px, 8=32px, 12=48px). Updated `button.tsx`, `Sidebar.tsx`, `Header.tsx`, `AppShell.tsx`, `EmptyState.tsx`, `ErrorState.tsx`, `LoadingState.tsx` accordingly. **Any future component must use numeric Tailwind spacing utilities (`p-3`, `gap-6`, etc.), never re-introduce named `--spacing-*` tokens.**
    2. shadcn's `Skeleton` used `bg-muted`, which is `#F5F5F5` — identical to the page background after 0.4.1's palette change, making every skeleton loader invisible. Fixed by changing `skeleton.tsx` to `bg-neutral-gray-light` (#CCCCCC) for real contrast.
    Also spent significant investigation on an apparent "toast never renders" failure that turned out to be a **transient Fast Refresh artifact** (caused by the rapid edit-and-reload cycle while fixing bug #1 above) — confirmed via isolated minimal reproductions (plain button + Toaster, then incrementally rebuilding the full composition) that sonner/React 19/Next 15.5 all work correctly together; a clean dev-server restart resolved it. Not a real code bug, but documented here in case it resurfaces.

In Progress:

- None — Phase 0 (0.1–0.4) is now fully complete per task-breakdown.md. Next: Phase 1 — Authentication & User Registration (1.1 Backend Auth Module).

Blocked:

- None

Note:
- `backend/.env.example` masih pakai format DIRECT_URL lama (pooler-based) — file `.env*` di-block oleh permission settings, gak bisa diedit otomatis oleh Claude. User perlu update manual kalau mau `.env.example` konsisten dengan `.env` yang sekarang jalan.
- **Security advisory (Supabase MCP):** RLS disabled di seluruh 27 domain tables + `_prisma_migrations` (28 total). User memutuskan ditunda sampai auth/RBAC phase diimplementasi (policy gak bisa didefinisikan dengan benar sebelum auth middleware ada). Jangan lupa revisit ini sebelum production / sebelum frontend punya kode yang akses Supabase langsung.
- **RESOLVED — DATABASE_URL pooler issue.** Penyebab: shard Supavisor salah di `.env` (`aws-0-ap-south-1` vs yang benar `aws-1-ap-south-1`). User update manual `backend/.env` dengan string dari dashboard, Claude verifikasi koneksi via test script `pg` sementara (sudah dihapus). 0.3.5 (Prisma Client Singleton) sekarang bisa lanjut pakai pooled connection.
