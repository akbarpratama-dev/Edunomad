import { VerifyView } from "@/components/artifact/VerifyView";

// Public certificate verification landing (Workflow 18) — no auth required.
export default function VerifyPage() {
  return <VerifyView />;
}
