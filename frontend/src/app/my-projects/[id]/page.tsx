// Project detail under the "Proyek Saya / Proyek Mentoring" section. Reuses the
// same page as /projects/:id; the component reads usePathname to keep navigation
// inside /my-projects when reached from here.
export { default } from "../../projects/[id]/page";
